"""Reed source adapter (UK remote roles).

Future work, see plan section 12. Reed uses HTTP Basic auth with the API key as the
username and an empty password. Implement ``fetch`` analogously to the other adapters
when v1 is stable.
"""
