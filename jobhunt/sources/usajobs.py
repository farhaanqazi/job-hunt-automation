"""USAJOBS source adapter (US public-sector remote roles).

Future work, see plan section 12. USAJOBS requires these request headers:

    Host: data.usajobs.gov
    User-Agent: <USAJOBS_EMAIL>
    Authorization-Key: <USAJOBS_API_KEY>

Implement ``fetch`` analogously to the other adapters when v1 is stable.
"""
